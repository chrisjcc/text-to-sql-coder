# Core dependencies
torch>=2.0.0
transformers>=4.40.0
datasets>=2.14.0
peft>=0.10.0
trl>=0.8.0
accelerate>=0.27.0
bitsandbytes>=0.42.0

# Verifiers library
verifiers>=0.1.0

# SQL validation
sqlparse>=0.4.4
sqlglot>=20.0.0

# Utilities
python-dotenv>=1.0.0
wandb>=0.16.0
huggingface-hub>=0.20.0

# Optional: Flash Attention (better performance)
# flash-attn>=2.5.0  # Install separately: pip install flash-attn --no-build-isolation

# Development
pytest>=7.4.0
black>=23.0.0
flake8>=6.0.0
mypy>=1.5.0
